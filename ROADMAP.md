# CJ 后续功能实现规划

状态：MVP 已完成，M1 交互会话正在实现。以下是 MVP 之后的路线图；未标记为已实现的内容仍是规划目标。

当前已实现的 M1 切片：`cj chat` 多轮上下文、本地会话命令、readline 输入历史、粘贴多行合并、单轮超时/取消，以及 `--plain`/`--no-color`。Markdown block 缓冲、批量确认、PTY 跨平台验收仍待后续迭代。

## 1. 产品原则

- Tool 负责定义具体操作语义，Agent 不针对某一种删除、写入或外部服务做特殊判断。
- 模型只负责理解任务和选择 Tool，路径边界、风险、确认、超时和审计继续由本地 CLI 强制执行。
- API Key 不进入模型上下文、子进程环境、终端日志或审计正文。
- JSONL 输出保持机器可读兼容；人类终端输出可以持续优化。
- 默认不启用遥测、长期记忆或远程插件；新增能力必须显式启用。
- 每个阶段都必须有单元测试、集成测试和跨平台验收标准。

## 2. 里程碑总览

| 里程碑 | 主题 | 目标版本建议 | 依赖 |
|---|---|---:|---|
| M1 | 更好的交互体验 | 0.2 | 当前 MVP |
| M2 | 任务控制与会话管理 | 0.3 | M1 |
| M3 | 多 Provider 与 Profile | 0.4 | 当前配置层 |
| M4 | Tool 扩展机制 | 0.5 | M3、M1 |
| M5 | 安全策略与可恢复执行 | 0.6 | M2、M4 |
| M6 | 本地可选记忆 | 0.7 | M2、M5 |
| M7 | 发布与生态完善 | 1.0 | M1-M6 中的稳定部分 |

版本号是发布建议，不承诺具体日期。

## 3. M1：更好的交互体验（最高优先级）

目标：让 `cj` 更像一个稳定的终端助手，而不是“执行一次命令后退出的 Tool 调度器”。

### 3.1 交互会话

新增独立的交互入口，例如 `cj chat`，避免改变现有 `cj <prompt...>` 的脚本兼容性。

功能：

- 在同一进程中连续提出多个问题，保留当前会话上下文。
- 提供 `/clear`、`/status`、`/tools`、`/history`、`/exit` 等本地命令。
- 支持方向键、输入历史、多行输入和粘贴内容。
- `Ctrl+C` 优先取消当前模型请求或 Tool；再次触发才退出会话。
- 非交互输入继续保持单次任务模式，不进入 REPL。

验收标准：

- `cj chat` 在 TTY 中进入会话，管道输入不会意外等待交互。
- 上一个回答的完整内容可以被下一轮引用，但 API Key 和敏感文件内容不会自动进入新一轮上下文。
- 会话退出后不保存长期记忆；仅记录现有的脱敏审计元数据。

### 3.2 终端输出与 Markdown

- 继续完善标题、列表、引用、代码块、表格和链接的终端渲染。
- 流式输出按 Markdown block 缓冲：普通段落可以渐进显示，未闭合的代码块不会破坏布局。
- 根据终端宽度换行，正确处理中文和宽字符。
- 增加 `--plain`、`--no-color` 和可选 pager，方便日志、SSH 和窄终端使用。
- Tool 事件使用统一的状态图标、耗时、风险颜色和可折叠详细信息。
- 错误信息显示恢复建议，但不显示凭据、完整环境值或不必要的模型原文。

验收标准：

- `**bold**`、代码围栏和 GFM 表格不再以 Markdown 标记原样出现。
- `NO_COLOR=1`、重定向到文件和 Windows 终端都能得到可读输出。
- JSONL 的事件顺序、字段和原始增量内容保持兼容。

### 3.3 确认体验

- 保留当前高风险操作面板和 `y/n` 确认。
- 多个连续的同一高风险动作可以显示为一个批次摘要，但每个 action 仍由本地策略逐个授权。
- 明确展示“将执行什么、影响哪些目标、是否可恢复、拒绝如何处理”。
- 永不引入默认批准的 `--yes`，也不允许模型消息代替用户确认。

### 3.4 M1 交付物

- `src/cli/session/` 会话状态和本地命令。
- `src/cli/renderers/` 统一的 Markdown、Tool 状态和确认面板渲染。
- TTY/PTY 集成测试，覆盖 macOS、Linux、Windows。
- README 增加会话模式、纯文本模式和终端兼容说明。

## 4. M2：任务控制与会话管理

目标：让长任务可观察、可取消、可重试，而不会丢失安全边界。

- 为每个任务提供短 ID、开始时间、当前阶段和最终状态。
- 增加 `/cancel`、`/retry`、`/last` 等会话级控制。
- 模型请求、Tool 执行和整体任务分别有超时、取消和输出上限。
- 对失败的 Tool 显示结构化失败原因；重试前重新 prepare 和重新评估风险。
- 增加任务状态事件：queued、planning、waiting_confirmation、running、completed、failed、cancelled。
- 仍然限制最大 Tool 调用数，不因重试而绕过总额度。

验收标准：取消后不会启动新的 Tool；重试不会复用已过期或已授权的 action；审计记录能还原任务生命周期。

## 5. M3：多 Provider 与 Profile

目标：保留 DeepSeek 默认体验，同时支持多个 OpenAI-compatible 服务。

- `cj config profile list|use|add|remove` 管理多个配置档。
- 每个 Profile 保存 Provider ID、API 地址、模型、thinking 设置和限额。
- 支持 API Key 文件和环境变量引用两种凭据方式，沿用现有权限检查和脱敏规则。
- Provider 适配器独立实现，不把 DeepSeek 特有字段泄漏到通用协议。
- `cj doctor --profile <name>` 分别检查配置、凭据和服务连通性。
- 支持非交互配置命令，便于 CI 和 dotfiles 自动化，但不接受命令行明文 API Key 参数。

验收标准：切换 Profile 不会串用凭据；错误信息能指出 Profile 和 Provider，但不泄露密钥；所有 Profile 仍经过同一 Agent 和 Tool 策略。

## 6. M4：Tool 扩展机制

目标：让用户可以增加自己的 Tool，同时不削弱宿主安全模型。

### 6.1 本地 Tool SDK

- 发布版本化 Tool 接口、输入 schema、风险级别、效果声明和 prepared action API。
- 支持从用户级目录发现本地 Tool，并提供 `cj tools list|show|doctor` 检查。
- Tool manifest 必须声明版本、权限需求、平台兼容性和数据流向。
- Tool 运行在宿主策略之后；插件不能绕过路径、确认、超时、输出和审计。

### 6.2 MCP（后续可选）

- 在本地 Tool SDK 稳定后再接入 MCP。
- 每个 MCP Server 单独配置、单独授权和单独审计。
- 默认禁用网络型 Server；连接地址、传输方式和外部数据流必须在配置中可见。
- 不把远程 Tool 的描述当作安全策略，仍由本地宿主做最终判定。

验收标准：恶意 Tool 描述不能扩大工作区权限；未声明的副作用不能降低风险；禁用插件后历史任务仍可查看。

## 7. M5：安全策略与可恢复执行

目标：在功能扩展后继续保持“模型不是安全边界”。

- 支持用户设置的目录授权范围，但不能超过编译时宿主上限。
- 增加统一 dry-run/preview 能力，实际执行仍必须再次检查目标和 action 有效期。
- 为可恢复 Tool 记录快照、结果和恢复提示；不由宿主臆造 Tool 的回滚语义。
- 对多步骤任务增加依赖图和部分失败报告，明确已完成、未执行和需要人工处理的步骤。
- 强化 symlink、junction、UNC 路径、大小写和 TOCTOU 测试。
- 增加审计导出和清理策略，仍不导出 prompt 原文、文件正文或凭据。

验收标准：任何扩展 Tool 都不能绕过 host policy；确认前后目标变化会触发重新预览；失败报告不会误称操作已回滚。

## 8. M6：本地可选记忆

目标：在用户主动开启的前提下提供可控的长期偏好和事实记忆。

- 默认关闭，提供 `cj memory on|off|list|forget`。
- 记忆内容与任务审计分离，默认只保存用户明确确认的短文本事实或偏好。
- 敏感数据、API Key、文件正文、命令输出和外部凭据不得自动进入记忆。
- 每次使用记忆时显示来源和用途，允许单条删除和全部清空。
- 先采用本地可审计存储，再评估索引或向量检索；不引入默认云端同步。

验收标准：关闭记忆后新任务不读取历史偏好；删除后无法被下一轮检索；记忆库文件拥有与 auth/history 相同的本地权限保护。

## 9. M7：发布与生态完善

- 发布 npm stable 版本并维护变更日志。
- 提供 macOS/Linux/Windows 安装和升级文档。
- 增加 Bash、Zsh、Fish、PowerShell 补全。
- CI 覆盖 Node LTS、三平台、类型检查、全量测试、构建、打包和依赖审计。
- 提供版本诊断、迁移脚本和配置 schema 迁移机制。
- 评估 Homebrew、winget 等渠道，但不复制用户的凭据文件。

## 10. 测试路线

每个里程碑都需要增加以下测试：

- 确定性 Fake Provider 测试，不依赖真实模型输出。
- Provider 协议测试，覆盖流式文本、分片 Tool 参数、错误和取消。
- Tool schema、路径边界、敏感数据、确认和 action 过期测试。
- PTY/TY 测试，验证真实终端下的颜色、换行、交互和 Ctrl+C。
- macOS、Linux、Windows CI；Windows 重点覆盖 ACL、PowerShell、junction 和回收站适配。
- 可选真实 DeepSeek smoke test，默认不发送计费请求。
- 发布包安装测试，验证 npm 用户拿到的 `cj` 与源码构建一致。

## 11. 建议实施顺序

1. M1：交互会话、终端 Markdown、Tool 状态和确认体验。
2. M2：任务 ID、取消、重试和生命周期事件。
3. M3：多 Provider、Profile 和非交互配置。
4. M4：本地 Tool SDK，再评估 MCP。
5. M5：权限范围、dry-run、可恢复执行和安全回归。
6. M6：默认关闭的本地记忆。
7. M7：稳定发布、安装渠道和 Shell 补全。

在 M1 和 M2 稳定之前，不建议引入长期记忆或远程插件；它们会同时放大交互复杂度、权限边界和排错成本。
